import { Record } from 'immutable';
import { IAnyValue } from './any';
export interface INumberValue extends IAnyValue {
    type: 'number';
    content: number;
}
declare const NumberValue_base: Record.Factory<INumberValue>;
export default class NumberValue extends NumberValue_base {
}
