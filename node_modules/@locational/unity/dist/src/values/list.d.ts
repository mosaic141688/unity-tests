import { Record, List } from 'immutable';
import { TAnyValue, IAnyValue } from './any';
export interface IListValue extends IAnyValue {
    type: string;
    content: List<TAnyValue>;
}
declare const ListValue_base: Record.Factory<IListValue>;
export default class ListValue extends ListValue_base {
}
