import { Record, Map } from 'immutable';
import { IAnyValue, TAnyValue } from './any';
export interface TRecordValue extends IAnyValue {
    type: string;
    content: Map<string, TAnyValue>;
}
declare const RecordValue_base: Record.Factory<TRecordValue>;
export default class RecordValue extends RecordValue_base {
}
