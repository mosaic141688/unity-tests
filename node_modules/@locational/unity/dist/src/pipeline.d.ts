import { ITransform, ITransformConfig } from "./transforms/any";
import { TDepList } from "./expression/parser";
export declare type TPipeline = IPipelineSection[];
export declare type TPipelineConfig = ITransformConfig[];
export interface IPipelineSection {
    input_type: string;
    output_type: string;
    deps: TDepList;
    transform: ITransform;
}
export declare function section(input_type: string, config: ITransformConfig): IPipelineSection;
export declare function pipeline(input_type: string, config: TPipelineConfig): TPipeline;
export declare function extractPipelineConfigDeps(config: TPipelineConfig): TDepList;
