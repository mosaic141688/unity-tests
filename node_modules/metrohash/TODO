- encoding (digest)
- generalize Update and Hash argument processing

const metrohash = require('metrohash');

let hasher64 = metrohash.createHash('metrohash64', [ seed ]);
let hasher128 = metrohash.createHash('metrohash128', [ seed ]);

hasher64.update('foo').update('bar').digest('hex')
